spring:
  application:
    name: QuestionService

  datasource:
    generate-unique-name: false
    name: questions
    url: jdbc:h2:mem:questions
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
